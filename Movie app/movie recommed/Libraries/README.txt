Libraries Folder

This folder contains all the required JAR files and external libraries for the project.

Current Libraries:
- gson-2.8.9.jar - Google's JSON parsing library for Java

The run-simple.bat script will automatically download this library if it's not present.

Note: Keep this folder clean and only add necessary libraries.
